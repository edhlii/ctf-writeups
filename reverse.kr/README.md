# Easy Crack - 100 points

This is an windows binary compiled in C++. Idk why Ghidra suck at decompiling C++ file, so I just lazy and use IDA Pro.

Obtain it via `hexrays.su` or `auth.lol`

Open the binary with IDA Pro, I find the `WinMain` function. Pretty straight forward => I get the flag.

![alt text](image.png)

Flag: `Ea5yR3versing`